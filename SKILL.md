---
name: presentation-review
description: 对已有 PPT/PPTX、PDF、逐页截图、大纲或讲稿进行系统审查。在用户要求评审、检查或诊断演示文稿并输出问题时使用；先咨询受众、使用场景、时长与审查范围，再统一准备证据，强制检查跨页母版、布局、Logo、标题和字体系统，按综合或专项范围调度八个维度。若用户声明涉外、对外发布、客户交流、外部共享或保密要求，还检查正文、图表、备注、隐藏内容和元数据中的敏感信息是否适合对外展示；最终按中文序号维度输出有证据的问题，并标注必要的待确认和未评估范围。
---

# PPT 审查总控

`dimensions/*/SKILL.md` 是本总控内部调用的详细规则模块，不单独触发、不单独生成报告；本目录唯一的技能入口是本文件。

## 核心职责

将本文件作为编排器，不在这里复制各维度的详细规则。完整执行以下工作：

1. 说明当前输入可以支持的审查范围；
2. 建立唯一的页码、对象、文本、渲染和来源证据包；
3. 在模块调度前完成一次跨页母版/布局/重复构件/标题系统回归；
4. 要求“页面主旨与文字”模块对每页的标题、分区、卡片、图示标签和总结进行同页语义冗余比较；跨页统计不能替代这项检查；
5. 沿同一条阅读因果链运行综合范围或用户选定的维度模块；
6. 用一套边界规则合并重复结论；
7. 仅按维度输出子问题；保密相关问题归入第八维度，不新增第九维度。

默认只审查，不修改原文件。不划分 PPT 类型，不套固定叙事模板，不使用规则 ID、评分或 AI 检测器通过率。

## 漏检复盘与硬性改进

上次评审并非完全没有读取母版、布局或全页渲染，而是把“提到要检查”误当成了“完成跨页比较”：没有生成每页映射矩阵、重复 Logo 资源/坐标聚类、同级标题样式聚类，也没有把页面级覆盖与母版继承区分出来，聚合时因此优先保留了内容和密度问题。这是工作流约束不足，不是可接受的人工疏忽。

从本版本起，PPTX 在进入维度模块前必须生成并读取跨页系统证据。没有这些产物，不得声称“母版统一”“标题统一”或“未发现一致性问题”；只能写入“未评估：未完成跨页系统统计”。

## 审查链路

```text
任务定义：目标与任务
        ↓
内容成立：内容与证据 → 论证与页面链路
        ↓
页面表达：页面主旨与文字 → 视觉语义与版式 → 可读性、密度与节奏 → 中文表达与 AI 味
        ↓
交付可靠：文件工程、可访问性与交付
```

```text
目标与任务
→ 内容与证据
→ 论证与页面链路
→ 页面主旨与文字
→ 视觉语义与版式
→ 可读性、密度与节奏
→ 中文表达与 AI 味
→ 文件工程、可访问性与交付
```

上图是四段审查链，下图是具体调度顺序。它们都是审查依赖关系，不是修改顺序。前一模块交付后一模块必需的已确认上下文，后一模块不得重新臆测目标、受众或事实。

## 调用前咨询与范围确认

在开始证据准备或审查前，先读取 [启动咨询模板](references/intake-template.md)，向用户发送其中的固定文案。用户可以补充信息，也可以回复“开始”“按默认”或“跳过，直接开始”；**不主动询问“目的与期望结果”**。沟通任务和期望结果由技能结合封面、标题、备注、页面链路和用户已有上下文推断，无法确认时标为 `Needs confirmation`。

执行以下规则：

1. 原样发送 `references/intake-template.md` 的用户可见文案，展示八个审查维度及每个维度的一句话说明，并询问“综合审查”还是“专项审查”；不自行改写成 PPT 类型或叙事模板。
2. 若当前消息已经完整提供受众、场景、时长和范围，不重复提问，直接按模板格式回显并请求确认。
3. 用户未填写范围、明确说“全面/综合/全部”或只说“帮我审查”时，设置 `review_scope=all`，调度八个维度。
4. 用户选定一个或多个维度时，设置 `review_scope=selected`，最终报告只渲染所选维度；为保证判断质量，仍可在内部读取必要的前置上下文和执行必要的文件预检，但不输出未选维度的常规问题。
5. 用户用“排版、统一性、逻辑、数据、去 AI 味、字体/母版”等自然语言表达范围时，按模板中的映射表转换为维度，并在开始前回显转换结果；若一个说法映射到多个维度，明确列出全部映射结果。
6. 用户回答后先用可读摘要回显已识别的受众、场景、时长、范围、重点和约束，再询问是否开始；用户回复“开始”“确认”或“按默认”后进入证据准备。只在答案会显著改变结论、严重度或建议时追加一轮追问。
7. 未提供的字段填 `unknown`，依赖这些字段的判断标为 `Needs confirmation`；不得把用户未提供背景本身列为 PPT 缺陷。
8. 用户直接上传文件并要求评审、但未填写咨询项时，先发送模板；若用户明确要求立即开始或回复“按默认”，使用上述默认值继续，并在报告的覆盖说明或待确认项中标明缺失背景。

内部可用的上下文字段包括 `audience`、`delivery_mode`、`duration_minutes`、`constraints`、`review_scope`、`selected_dimensions`、`priority_focus`、`confidentiality_review`、`sensitive_value_policy` 和 `unconfirmed`；`purpose`/`expected_action` 只从材料和上下文推断，不作为启动前必答项。`confidentiality_review` 取 `enabled`、`not_requested` 或 `needs_confirmation`；`sensitive_value_policy` 取 `original_allowed`、`mask_required` 或 `minimal`。

### 保密要求的条件触发

- 用户明确提到“涉外”“对外发布”“客户交流”“外部共享”“保密审查”或要求检查敏感信息时，设置 `confidentiality_review=enabled`，调用第八维度中的对外展示敏感性规则。
- 保密审查开启且用户未另行要求时，设置 `sensitive_value_policy=original_allowed`，允许普通敏感事实、指标、名称、案例或技术描述用页面原值定位；用户明确要求打码、脱敏或不要回显时设置 `mask_required`；用户明确要求“只给最小片段”时设置 `minimal`。
- 用户明确说“仅内部使用”或“无需保密审查”时，设置 `confidentiality_review=not_requested`；仍做常规的个人信息、账号/密钥、内部路径、备注、隐藏对象和元数据残留扫描，但不把普通业务信息自动判定为机密。
- 用户没有提到保密场景时，设置 `confidentiality_review=needs_confirmation` 或 `not_requested`（按是否存在外部共享线索决定）；不得把“业务信息”本身直接判定为敏感。
- 该审查属于“八、文件工程、可访问性与交付”的条件审查，不作为第九个维度。保密审查必须逐页、逐对象定位信息，并说明它为何可能受限；用户已明确允许时，普通敏感事实、指标、名称、案例或技术描述可以在报告中保留页面原值，便于回查。密码、私钥、令牌、会话凭据等认证材料无论用户是否授权，都不得回显完整值，只描述类型和位置。

内部阶段按 `INTAKE_PENDING → INTAKE_CONFIRMED → EVIDENCE_PREPARED → REVIEW_RUNNING → REPORT_READY` 推进；用户修改字段时回到 `INTAKE_CONFIRMED`，不得沿用旧范围或旧约束直接出报告。

## 输入范围与证据准备

### 声明可审查能力

- `.pptx`：尽量检查文本、备注、对象、几何、母版、链接、可编辑性、文件包和全页渲染；
- `.pdf`：检查可提取文本与渲染，不声称检查备注、母版、原生对象或 OOXML；
- 截图：只检查可见文本、视觉层级、语义、密度和一致性；
- 大纲或讲稿：检查目标、内容、论证、页面主旨和中文表达。

### 只建立一份共享证据包

先使用当前环境可用的 PPT 读取和渲染能力。所有模块共用同一份结果，不各自重新编页或以不同渲染结果下结论。证据包包含：

- **上下文**：受众、目标、期望结果、交付方式、时长、约束及未确认项；
- **页面**：唯一页码、页数、比例、尺寸、隐藏页、空页、标题串和页面顺序；
- **文本**：每页文本、备注、标签、图注、来源、链接、替代文本、术语串和数字串；
- **对象**：文本框、图形、图表、表格、图片、图标、媒体、组合和嵌入对象及其位置、大小、层级；
- **渲染**：全部页面的图像或 PDF，以及裁切、溢出、重叠、缺字体或缺媒体现象；
- **系统**：字体、字号、颜色、对齐、间距、母版、布局、页脚和重复构件统计；另含跨页系统回归产物（见下节）；
- **文件**：PPTX 包关系、媒体、外链、字体状态、图片化对象、占位符、批注、隐私和模型残留。

证据包只记录可观察事实和工具限制，不预先写评价。

### 能力状态标记

证据包中每一类字段都要标记状态：

- `Available`：已提取并可靠可读；
- `Not available`：输入格式或工具不支持；
- `Not checked`：理论上可检查，但本次未执行；
- `Needs confirmation`：需要作者、来源或目标环境确认。

模块必须先读取这些状态：仅对依赖 `Not available` 或 `Not checked` 字段的检查返回“未评估：原因”，其他 `Available` 字段照常审查，并在未审查内容中说明限制；字段为 `Needs confirmation` 时，可输出标明置信度的待确认问题。

### 证据包失败与降级

依次尝试当前环境可用的文件读取、文本提取、对象盘点和全页渲染能力。每一次失败都记录工具、输入层和失败原因；不得凭空声称已检查。

- 能提取文本但不能渲染：可运行目标、内容、论证和部分文案审查；视觉、密度和文件渲染风险标记未评估；
- 能渲染但不能读取对象：可运行可见层级、视觉语义和密度审查；文件包、可编辑性和对象几何标记未评估；
- 既无可解析文本也无渲染：只返回输入和工具限制，不产生模板化审查结论。

### 跨页系统一致性硬门槛（PPTX 必做）

这一步不是审美抽查，而是文件工程与视觉版式的共同证据准备。必须一次性生成下列五项产物，并把结果放入共享证据包：

1. **页面—母版—布局—主题映射矩阵**：逐页记录实际使用的母版、布局、主题、占位符来源和页面级直接格式覆盖；不能只统计文件里“存在”的母版。
2. **重复构件清单与聚类**：识别 Logo、页眉、页脚、页码、章节条、固定徽章等重复构件，记录资源路径或图片哈希、对象类型、归一化 `x/y/w/h`、透明度、层级和出现页码。图片文件名不同但视觉相同，要用哈希/感知相似或渲染对照归并。
3. **同级标题样式聚类**：对标题候选（标题占位符或页面上方的大字号文本框）记录文本框 `x/y/w/h`、字体家族、字号、粗细、颜色、对齐、行距、占位符/手工文本框来源和出现页码；按同功能/同版式族比较，不把封面或章节页等明确特例混入。
4. **继承与覆盖统计**：分别统计母版/布局继承对象和页面层手工对象；记录同一构件被页面覆盖、复制或移动的比例，以及覆盖对象是否使用不同资源或样式。
5. **偏差与例外表**：按同功能页面聚类出位置、尺寸、字体、字号、资源的离群值；每个离群值标注“有意特例 / 作者确认 / 未解释”，未确认的系统性偏差必须进入候选问题池。

至少执行一次源对象与渲染结果的对照。以下信号只作为候选，不可脱离渲染直接定性，但出现成簇时必须升级审查：同一 Logo 使用两套资源；同一功能页的 Logo 或标题归一化坐标/尺寸明显分散（约超过页面宽高的 1% 或重复出现的可见位置差异）；同级标题混用字体家族、字号或占位符/手工来源；同一母版要求被页面直接格式反复覆盖。主问题归入“文件工程、可访问性与交付”，视觉模块只补充观众可见的层级或阅读后果，避免重复。

若用户明确提供品牌规范，按规范作为硬约束；未提供时仍可报告已确认的跨页漂移，但把“是否有意特例”标为 `Needs confirmation`，不凭个人审美判错。

### 页数分档与效率控制

默认审查仍是八个维度、逐页深审，不因快速筛查或页数增加而跳过普通页面。效率优化只针对重复取证：所有模块只读取同一份 evidence manifest，禁止重复解析 XML、重复提取文本或重复渲染；快速筛查只用于提前标记异常页和对象、安排复核顺序，不能作为跳过页面或维度详细规则的依据。

对 PPTX 优先运行 [scripts/build_evidence.py](scripts/build_evidence.py) 生成 `evidence.json`、`system-matrix.json`、`review-index.json` 和 `slide_text.txt`，再补充一次全页渲染/接触表。脚本默认递归解析组内对象，并纳入布局/母版形状、Logo/chrome 与标题的容差离群统计；`review-index.json` 是不含页面正文的轻量导航索引，记录输入文件哈希、可复用缓存键、异常页、隐藏页/对象和需要重点定位的对象；`geometry_overflow_candidates` 只表示对象几何越界，不等同于文本溢出。示例：

```bash
SKILL_DIR=/path/to/presentation-review
python3 "$SKILL_DIR/scripts/build_evidence.py" input.pptx evidence/
```

需要调整漂移敏感度时使用 `--drift-threshold 0.01`（归一化页面尺寸，默认 1%）；只有在布局/母版无法读取时才使用 `--no-inherited`，并保留输出中的 `Not checked` 状态。脚本不负责渲染、目标 Office 播放或正式无障碍认证；这些层必须由调用者补充并写入报告覆盖说明。

脚本不可用时，必须用一次等价的确定性命令生成同样的页面—母版—布局矩阵、Logo/chrome 聚类、标题样式统计和轻量异常索引，并在覆盖说明中写明降级原因；不要让八个模块各自手工重复提取。

| 页数 | 必做自动检查 | 逐页深审 |
|---|---|---|
| `≤30` | 全页文本/对象/几何/字体/媒体/备注/隐藏页/母版布局/Logo/标题聚类；全页渲染并生成接触表与 `review-index.json` | 接触表全览后，逐页执行八维度详细规则；索引标记的异常对象优先定位 |
| `31–80` | 上述项目全部全量执行；输出页面特征和异常索引 | 接触表全览后，逐页执行八维度详细规则；复用同一证据包，不重复解析或渲染 |
| `>80` | 上述项目仍全量执行；按章节和异常信号生成导航索引 | 仍逐页执行详细规则；可按索引安排页面顺序，但不得以抽样替代逐页审查 |

报告文首必须写明 `审查范围`、`全量自动检查`、`缩略图全览`、`全尺寸检查页码`、`抽样规则`和`未检查层`；专项审查列出实际选择的维度。默认逐页深审时，`全尺寸检查页码`应记录实际完成的全部页面；`review-index.json` 中的异常页和对象只用于导航，不构成抽样豁免。长 PPT 会增加逐页审查的线性成本，但不应因重复解析、重复提取或重复渲染而额外放大。

## 按范围调度八个维度

综合审查时，必须按以下顺序逐一读取并执行全部模块；专项审查时，只执行用户选定的模块及其必要前置依赖，并禁止把前置模块的常规问题渲染到最终报告：

1. [目标与任务](dimensions/purpose-and-task/SKILL.md)
2. [内容与证据](dimensions/content-and-evidence/SKILL.md)
3. [论证与页面链路](dimensions/argument-and-flow/SKILL.md)
4. [页面主旨与文字](dimensions/slide-message-and-copy/SKILL.md)
5. [视觉语义与版式](dimensions/visual-semantics-and-layout/SKILL.md)
6. [可读性、密度与节奏](dimensions/readability-density-and-pacing/SKILL.md)
7. [中文表达与 AI 味](dimensions/chinese-expression-and-ai-patterns/SKILL.md)
8. [文件工程、可访问性与交付](dimensions/file-accessibility-and-delivery/SKILL.md)

每个实际执行的模块在内部返回一个统一对象，只包含：`problems[]`、`confirmed_context[]`、`needs_confirmation[]`、`not_assessed[]`。`confirmed_context` 等交接字段只在模块间传递，不直接渲染到用户报告。模块不输出分数、优点、独立摘要、逐页报告或建议修改顺序。

### 模块加载与失败协议

按调度顺序一次加载一个模块，将共享证据包、能力状态和前序模块的只读交接对象传入。交接对象包含 `confirmed_context[]` 、`needs_confirmation[]` 和 `not_assessed[]`；后续模块不得将前置的待确认或未评估项升级为已确认。每个模块完成后先将输出转成统一对象，再传给下一模块；不将模块文案、规则说明或模块的过程性文字直接拼入最终报告。

- 模块文件不存在、无法读取或执行报错：记入该维度的 `not_assessed[]`，写明文件路径、失败原因和受影响范围，继续调度其他不依赖它的模块；
- 证据包缺少某一层：只允许该模块输出已有证据的问题，其余判定放入 `not_assessed[]`，不用经验补全；
- 后续模块缺少前置上下文：保留自己可独立确认的部分，将依赖项记为 `needs_confirmation[]` 或 `not_assessed[]`，不重新臆测前结论；
- 所有模块都无法获取文本、渲染或可靠背景时：输出输入/工具限制和未评估范围，停止生成没有证据的审查结论。

### 模块交接

| 模块 | 主要输出 | 供后续使用的上下文 |
|---|---|---|
| 目标与任务 | 受众、任务、期望结果、待确认背景 | 所有后续模块的判断基准 |
| 内容与证据 | 论断、口径、来源、不确定项、矛盾 | 论证、标题、图表和语气的事实约束 |
| 论证与页面链路 | 页面功能、前后依赖、断裂和决策封口 | 页面主旨、表达、密度和视觉关系 |
| 页面主旨与文字 | 当页主旨、标题/正文不匹配、文案层级 | 视觉语义、可读性、中文与文件检查 |
| 视觉语义与版式 | 关系编码、图形错配、层级与跨页视觉语法 | 密度、阅读路径、文字与交付风险 |
| 可读性、密度与节奏 | 扫读、文字负荷、观看距离、页间重复 | 中文模式、字体/渲染和交付条件 |
| 中文表达与 AI 味 | 具体性、语气、重复句式、模型残留 | 最终去重、待确认和报告问题 |
| 文件工程、可访问性与交付 | 包、渲染、几何、可编辑、可访问和隐私风险 | 最终交付限制与未评估说明 |

## 问题归类边界

用下列问题决定主维度：

- 为什么讲、给谁、要得到什么结果？→ **目标与任务**
- 这句话、数字或结论凭什么？→ **内容与证据**
- 为什么这一页接在这里，它如何推进前后结论？→ **论证与页面链路**
- 这一页究竟说什么，文字是否一眼明白？→ **页面主旨与文字**
- 图形和空间是否把比较、流程、层级、因果等关系画对？→ **视觉语义与版式**
- 观众在正常距离和时间内能否看完、找到重点，整套是否过度机械重复？→ **可读性、密度与节奏**
- 中文是否具体、自然、有作者信息，而非空泛、套话和模板化组合？→ **中文表达与 AI 味**
- 文件能否稳定打开、呈现、编辑、访问和共享？→ **文件工程、可访问性与交付**

同一页不同区域是否在重复同一主张、但没有新增证据或信息增量？→ **页面主旨与文字**；跨页的句式或总结模板重复仍由“中文表达与 AI 味”模块主责。

同一问题同时命中多个维度时，将它放入最能指向根因和修正动作的主维度；其他维度不重复建问题，必要的后果直接并入主问题句或证据句。

### 重叠时的主责优先级

| 可观察现象 | 主责维度 | 其他维度处理方式 |
|---|---|---|
| 目标、受众、内容取舍与任务错位 | 目标与任务 | 不在其他维度重复 |
| 数字、口径、来源、因果或比较不成立 | 内容与证据 | 不在其他维度重复 |
| 页面顺序、前提、证据到决策的跳跃 | 论证与页面链路 | 不在其他维度重复 |
| 标题、正文、标签本身无法说清当页主旨，或同页多个非导航区域重复同一主张 | 页面主旨与文字 | 不在其他维度重复 |
| 比较、流程、因果、层级被错误画成卡片/图表/空间关系 | 视觉语义与版式 | 不在其他维度重复 |
| 信息量、字号、标签、观看距离使人看不完或找不到重点 | 可读性、密度与节奏 | 不在其他维度重复 |
| 抽象、重复、模板化句式与模型残留 | 中文表达与 AI 味 | 不在其他维度重复 |
| 字体、渲染、几何、导出、备注、辅助阅读和共享失败 | 文件工程、可访问性与交付 | 不在其他维度重复 |

## 聚合、去重与取舍

各模块完成后：

1. 统一页码、对象名称、术语和数据口径；
2. 合并“同一根因 + 重叠证据 + 相同修正动作”的问题；
3. 将跨页重复问题合成一条，在位置中并列页码，不再建“跨页问题”章节；
4. 保留能指向可观察后果的问题，并把后果写进“问题”或“证据”句，删除纯审美偏好、重复建议和无证据形容词；
5. 默认报告 P0–P2；只在 P3 重复成系统、会影响交付，或用户明确要求全量审查时保留；
6. 同一维度内按 P0 到 P3，再按首个出现页码排列。

## 严重度与置信度

严重度只写在单条问题内，不生成汇总表：

- `P0 阻断`：文件无法使用、关键信息不可见，或有可直接确认的重大错误；
- `P1 重大`：主要结论、推理、关键证据或必要行动无法可靠理解；
- `P2 重要`：明显降低理解、可读性、可信度、专业度或一致性；
- `P3 建议`：局部润色或细节优化，不妨碍主要任务。

置信度：

- `Confirmed`：文本、数据、结构、渲染或文件检查可直接确认；
- `Inferred`：多个可观察信号支持，但仍有其他合理解释；
- `Needs confirmation`：需要作者提供业务背景、来源、口径或交付约束才能定论；
- `Not assessed`：当前输入或工具不支持检查。

P0/P1 必须绑定精确页码、对象或文件检查证据。没有证据的猜测不得写成问题。

## 最终报告契约

读取 [报告模板](references/report-template.md)。除文件、实际输入、检查覆盖和限制外，报告仅保留“中文序号维度 → 自然问题序号”结构：

- 只渲染有问题或未评估的维度；已评估且无问题的维度省略；模块返回的“未检出需要报告的问题”不进入用户报告；
- 未评估维度在对应维度下用一句话说明原因；
- 需要作者确认的问题放在其所属维度，标记 `Needs confirmation`；
- 维度固定使用“一、目标与任务”至“八、文件工程、可访问性与交付”；只渲染有问题或未评估的维度，维度编号不因省略而改写；
- 每个已渲染维度下用“问题1、问题2、问题3……”自然编号，编号只服务阅读，不是规则 ID；
- 每条问题只保留“问题、位置/范围、证据、建议、严重度/置信度”，不生成单独的“影响”字段；
- 不生成执行摘要、总体判断、优点、状态、分数、逐页意见、跨页专章、严重度汇总或单独修改顺序。

### 交付前格式自检

在发送报告前逐项检查：

- 维度标题使用“一、”至“八、”的固定名称，未按省略情况重新编号；
- 每个已输出维度的问题标题为“问题1、问题2……”并从 1 重新开始；
- 每条问题只有位置/范围、证据、建议、严重度/置信度四个下级字段，正文或证据已包含可观察后果；
- 报告中不存在 `### 子问题`、`影响：`、优点清单、总分、逐页日志或单独修改顺序；
- 文首已写检查覆盖、抽样规则和未检查层，且与实际执行一致。

## 能力来源

读取 [市场能力融合映射](references/market-capability-map.md)，了解每个参考技能的规则已落入哪个维度。这些来源仅是方法参考，不是运行时依赖。
